import { VenueHome } from "@/components/venue/venue-home";

/** P1 · Scan landing — venue + fixed table, language up front. The shared
 * VenueHome renders loading / invalid / ready states from the provider. */
export default function ScanLanding() {
  return <VenueHome />;
}
